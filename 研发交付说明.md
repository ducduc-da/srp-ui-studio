# SRP UI 页面研发交付说明

## 交付内容

- `app/`：页面与业务组件源码。
- `components/`：基础 UI 组件。
- `public/`：页面使用的静态资源。
- `hooks/`、`lib/`：公共逻辑。
- `package.json`、`pnpm-lock.yaml`：依赖与版本锁定。
- 同包 `UI规范/`：后续新增页面必须遵循的视觉、交互和文案规范。

`node_modules/` 和 `dist/` 属于本地生成内容，未放入交付包。不要直接复制本机的 `localhost:3000` 地址，它无法在其他电脑访问。

## 环境要求

- Node.js 22.13 或更高版本
- pnpm

## 本地启动

```bash
pnpm install
pnpm dev
```

启动后打开终端提示的本地地址，通常为 `http://localhost:3000/`。

## 构建检查

```bash
pnpm build
```

当前交付版本已完成构建检查。

## 研发 AI 开工要求

开始修改前，先完整阅读同包 `UI规范/README.md` 与 `UI规范/AGENTS.md`。复用现有组件及设计 token，不得自行新增另一套颜色、字号、圆角、阴影、导航或侧边栏结构。完成后按 `UI规范/06-验收清单.md` 自检。

## 主要页面入口

- 首页、智能问数、专题分析与资产中心：`app/kv-home.tsx`
- 正式问答与图表回答：`app/qa-task.tsx`
- 搜索聊天：`app/sidebar-header-tools.tsx`
- 侧栏悬浮操作：`app/sidebar-row-actions.tsx`

